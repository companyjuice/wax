// TODO: align naming with existing tree/graph theory concepts
// TODO: research tree merging strategies (e.g. linked list?!)

const treeMerger = (currentTree, newTree) =>
    newTree.children.map(children =>

    );

export default treeMerger;
